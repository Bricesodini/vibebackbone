# Test coverage — Ruff check cleanup

**Date**: 2026-07-14 14:35 Europe/Paris
**Scope**: 11 Core tools and `tests/test_status_dashboard.py`
**Mode**: DEV
**Verdict**: `READY`

## Critical paths identified

| Critical path | Change class | Visible coverage | Evidence |
|---|---|---|---|
| dashboard ordering and rendering | fixture calls, local rename, string prefixes | COVERED | dashboard + review-tier suites in the 122 focused tests |
| executor artifacts and gates | string prefixes, unused timer | COVERED | 10 direct executor tests |
| loop closure resolution | import relocation | COVERED | loop-closure and run-resolution suites |
| contract lint/runtime/router | unused locals/imports, string prefix | COVERED | contract-lint tests plus all-contract dry-run |
| project bootstrap output | string prefixes | COVERED | project-init suite and full P.R2 |
| context compaction and index | unused local/import | COVERED | direct compactor and index suites |

## Visible coverage status

- 122 focused tests pass across dashboard, review-tier, compactor, contracts,
  executor, loop closure, project init and index.
- `python tools/vbb-contract-runtime.py run --all --dry-run` exits zero with the
  existing PASS/PARTIAL/BLOCKED distribution unchanged.
- `ruff check tools tests` passes with zero findings.
- Full pytest and local CI remain required by P.R2 before closeout.

## Priority gaps

| Priority | Gap | Impact |
|---|---|---|
| P2 | LLM healthcheck has no direct deterministic suite | unused typing import removal is low risk, but provider subprocess behavior remains indirectly covered only |
| P2 | Multiservice lint lacks a named direct test suite | removed import and f-prefixes are mechanical; future rule behavior needs dedicated fixtures |

No P1 critical path is uncovered by this cleanup.

## Top tests recommended next

1. Add deterministic registry/provider fixtures for LLM healthcheck when that
   tool next changes behavior.
2. Add direct valid/invalid fixtures for each multiservice lint rule before a
   semantic rule change.
3. Add an exact snapshot for project-init success guidance only if downstream
   consumers begin parsing human-readable stdout.

## Unknowns / evidence limits

- External consumers of human-readable diagnostic strings are unknown; this
  patch preserves their exact contents.
- Line coverage was intentionally not measured; the audit maps behavioral paths
  affected by the diff.

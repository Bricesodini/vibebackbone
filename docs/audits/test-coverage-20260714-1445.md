# Test coverage — Ruff format baseline

**Date**: 2026-07-14 14:45 Europe/Paris
**Scope**: 29 formatted Python files under `tools/` and `tests/`
**Mode**: DEV
**Verdict**: `READY`

## Critical paths identified

The formatted set crosses contract lint/runtime, executor, credentials, gate
checks, project initialization, status tooling and their direct test suites.
Because the formatter is required to be semantics-preserving, the primary
critical path is equivalence of every parsed Python module before and after.

## Visible coverage status

- Ruff 0.13.1 reformatted 29 files; 4 were already conformant.
- AST comparison against commit `513eb2e` covers all 29 changed files and finds
  zero differences with source-location attributes excluded.
- `ruff format --check tools tests`: 33 files conform.
- `ruff check tools tests`: zero findings.
- Full pytest and local CI are mandatory before closeout.

## Priority gaps

| Priority | Gap | Impact |
|---|---|---|
| P3 | Comments and whitespace are not represented in the AST comparison | documentation placement could move while executable semantics remain identical |

No P1/P2 test gap is created by this format-only diff.

## Top tests recommended next

1. Run the full pytest suite after formatting.
2. Run contract runtime, architecture, loop closure and local CI through P.R2.
3. Avoid snapshot churn solely for whitespace; add snapshots only for a real
   downstream textual contract.

## Unknowns / evidence limits

- AST equivalence does not prove comment placement equivalence, so the diff
  remains reviewable as the formatter's deterministic output.
- No untracked Python source was included; scope is the canonical `tools/` and
  `tests/` command from ADR 0035.

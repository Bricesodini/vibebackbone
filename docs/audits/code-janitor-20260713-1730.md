---
date: 2026-07-13
scope: global tracked repository
skill: 1-vbb-code-janitor
verdict: PARTIAL
---

# Code Janitor — global normalization pass

## Context

Passe read-only sur les outils, tests, scripts, distributions et artefacts
actifs. Le dépôt est en mode `DISTRIBUTION`; aucun changement métier, de contrat
ou de structure n'est autorisé dans cette passe.

## Verdict

`PARTIAL` — 36 erreurs Ruff et plusieurs résidus locaux sont réparables, mais
les hooks et l'executor révèlent des signaux structurels qui sortent du Janitor.

## Findings (prioritized)

### JAN-01 — P1 — structure — M

- **Evidence** : deux installateurs (`install-framework-gate-hook.sh`,
  `install-vbb-pre-commit.sh`) écrivent des contrats de hooks différents ; le
  premier installe un nom non exécuté par Git par défaut, le second écrase
  `pre-commit`.
- **Risk** : activation partielle ou implicite des gates.
- **Recommended action** : décision structurelle puis un installateur unique ;
  ne pas fusionner mécaniquement pendant un Janitor.

### JAN-02 — P2 — duplication — S

- **Evidence** : `_yaml_load()` est défini deux fois dans
  `tools/vbb-executor.py` (lignes 82 et 499) ; mypy signale `no-redef`.
- **Risk** : une correction portée sur une définition peut être neutralisée par
  la seconde.
- **Recommended action** : supprimer la définition dupliquée après tests de
  caractérisation executor.

### JAN-03 — P2 — naming — S

- **Evidence** : `write_closEOUT()` dans `tools/vbb-executor.py` mélange la
  casse au milieu du symbole et n'a qu'un appel interne.
- **Risk** : recherche, lecture et convention Python dégradées.
- **Recommended action** : renommer mécaniquement en `write_closeout` dans un lot
  couvert par les tests executor.

### JAN-04 — P2 — dead-code — S

- **Evidence** : Ruff trouve 7 imports/variables inutilisés côté outils et 4
  variables inutilisées dans `tests/test_status_dashboard.py`; 25/36 findings
  sont autofixables.
- **Risk** : bruit et fausses dépendances.
- **Recommended action** : lot `ruff --fix` revu manuellement, sans unsafe fix,
  puis suite complète.

### JAN-05 — P2 — structure — S

- **Evidence** : `docs/runs/routing-fix-verification.md` est un artefact loose
  `PENDING` hors répertoire horodaté ; QOA-006 le suit déjà.
- **Risk** : confusion entre run actif et note historique.
- **Recommended action** : archiver ou rattacher à un run après décision ; ne pas
  le déplacer dans cette passe read-only.

### JAN-06 — P2 — config-sprawl — S

- **Evidence** : 3 `.DS_Store` et un `tools/__pycache__/` existent localement,
  mais `.gitignore` les exclut et aucun n'est suivi.
- **Risk** : bruit local uniquement, aucune dette distribuée.
- **Recommended action** : nettoyage local optionnel ; aucun commit.

### JAN-07 — P2 — debug-leftovers — S

- **Evidence** : Ruff signale 19 f-strings sans interpolation et deux variables
  `l` ambiguës ; `ruff format --check` signale 26 fichiers.
- **Risk** : style hétérogène et faible signal lors des reviews.
- **Recommended action** : séparer style pur, imports/variables et renommages ;
  ne pas mélanger avec le typage.

## Quick wins (≤ 60 minutes total)

1. Retirer imports/variables inutilisés signalés par Ruff, hors executor.
2. Retirer les f-strings sans interpolation.
3. Renommer les variables locales ambiguës `l`.
4. Nettoyer les fichiers générés locaux ignorés.
5. Rejouer Ruff, pytest, architecture lint et contract lint.

## Consolidation plan (max 7 steps)

1. Caractériser l'executor par tests.
2. Dédupliquer `_yaml_load` et normaliser `write_closeout`.
3. Décider le contrat unique d'installation des hooks.
4. Traiter le lot Ruff safe-fix.
5. Appliquer le formatage dans un commit distinct.
6. Traiter mypy outil par outil, en commençant par executor/runtime.
7. Reclasser QOA-006/QOA-007 avec preuves.

## Structural gaps detected

| Finding | Signal | Recommendation |
|---|---|---|
| JAN-01 | responsabilités concurrentes pour l'installation des gates | `1-vbb-tech-debt` — TD-102 |
| JAN-02/JAN-03 | executor critique non testé avant nettoyage | `1-vbb-tech-debt` — TD-104 |
| JAN-07 | absence de baseline de style canonique malgré outils présents | décision `1-vbb-conventions` puis éventuellement `1-vbb-formatter` |

## Unknowns / needs confirmation

- L'intention utilisateur pour le fichier loose routing n'est pas déductible.
- La promotion future Ruff/mypy au gate nécessite une décision humaine ; elle
  n'est pas implicite dans la demande de nettoyage.

## After this skill runs

Ouvrir d'abord les micro-boucles TD-101/TD-102/TD-104. Les quick wins Ruff sont
candidats à un FAST-STANDARD séparé après baseline et staging propres.

```yaml
FINAL_STATUS:
  elapsed_seconds: 420
  budget_initial: 180
  progress_emitted: true
  progress_count: 1
  extension_requested: false
  timeout_closeout_emitted: false
  verdict: COMPLETE
  files_touched:
    - docs/audits/code-janitor-20260713-1730.md
  tests_run:
    - ruff check tools tests
    - ruff format --check tools tests
    - mypy tools --ignore-missing-imports
  tests_missing:
    - executor characterization
  risks:
    - JAN-01
    - JAN-02
  open_points:
    - no cleanup applied by read-only skill
```

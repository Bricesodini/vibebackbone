# Test coverage — static CI promotion

**Date**: 2026-07-14 15:30 Europe/Paris
**Scope**: local/GitHub static-quality gate wiring and failure detection
**Mode**: DEV
**Verdict**: `READY`

## Critical paths identified

| Critical path | Risk | Visible coverage | Evidence |
|---|---|---|---|
| pinned dev tool installation | runner misses Ruff/mypy | COVERED | manifest pin assertions + workflow install assertion |
| remote gate presence | push/PR accepts static regression | COVERED | workflow assertions for all three module commands |
| local/remote parity | contributor passes locally but fails remotely | COVERED | local-script assertions for the same paths/order |
| actual failure detection | wiring exists but command cannot reject debt | COVERED | temporary violations return Ruff check=1, format=1, mypy=1 |
| recovery to clean baseline | destructive probe residue | COVERED | probe removed; all three canonical commands pass |

## Visible coverage status

- `tests/test_static_quality_ci.py`: three wiring/version tests pass.
- Controlled probe: each of the three commands returns exit 1 for its dedicated
  violation and the fixture is removed afterward.
- Local CI executes the three new checks successfully; its provisional run
  reports only the expected non-blocking warning for the not-yet-closed run.
- Remote Ubuntu/macOS execution is required after push before QOA-007 closes.

## Priority gaps

| Priority | Gap | Impact |
|---|---|---|
| P2 | Wiring tests are textual rather than a full GitHub runner emulator | YAML/provider semantics still require the real post-push matrix |

No P1 test gap remains once the real remote matrix is observed green.

## Top tests recommended next

1. Observe both GitHub matrix jobs after the promotion push.
2. Keep the controlled command-level failure proof in the run evidence; do not
   commit intentionally broken fixtures.
3. If CI structure becomes more complex, replace textual assertions with a
   dedicated workflow schema/parser test rather than broad snapshots.

## Unknowns / evidence limits

- Branch protection is external to repository files and not assessed.
- GitHub-hosted runner image drift is controlled only by the real matrix and
  pinned Python/tool versions, not by local tests.

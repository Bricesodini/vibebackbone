# Test coverage — mypy cleanup

**Date**: 2026-07-14 15:05 Europe/Paris
**Scope**: nine typed Core tools and dynamic router failure boundary
**Mode**: DEV
**Verdict**: `READY`

## Critical paths identified

| Critical path | Type change | Visible coverage | Evidence |
|---|---|---|---|
| contract runtime output enrichment | heterogeneous dict/list annotations | COVERED | contract tests and all-contract dry-run |
| router dynamic import failure | optional spec/loader guard | COVERED | new direct run-path test asserts exit 2 and diagnostic |
| credentials allow/finding reporting | distinct dataclass loop variables | COVERED | credentials suite |
| dashboard contract ratio | corrected float return annotation | COVERED | dashboard suite |
| architecture/index/compactor containers | explicit element types | COVERED | direct architecture, index and compactor suites |

## Visible coverage status

- `mypy tools`: success, zero errors across 16 source files.
- Ruff check and format remain clean.
- 77 focused tests pass, including the new router-load failure case.
- All-contract dry-run exits zero with its existing result distribution.
- Full pytest and local CI remain mandatory before closeout.

## Priority gaps

| Priority | Gap | Impact |
|---|---|---|
| P2 | Multiservice empty-container annotations have no dedicated fixture suite | no runtime value changed, but future rule changes need direct tests |

No P1 critical path remains uncovered in this scope.

## Top tests recommended next

1. Add direct multiservice lint fixtures before changing any rule semantics.
2. Preserve the router-load exit-code test if the CLI main block is later
   extracted into a function.
3. Consider TypedDict tests only if runtime result dictionaries become a public
   schema; current `Dict[str, Any]` remains internal and evidence-backed.

## Unknowns / evidence limits

- Static annotations are erased at runtime; behavioral safety comes from the
  focused suites, dry-run and P.R2 rather than mypy alone.
- External consumers of internal Python helper annotations are unknown; no
  runtime signature or serialized output changed.

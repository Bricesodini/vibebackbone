# Test coverage — executor cleanup

**Date**: 2026-07-14 14:06 Europe/Paris
**Scope**: `tools/vbb-executor.py`, `tests/test_executor.py`
**Mode**: DEV
**Verdict**: `READY`

## Critical paths identified

| Critical path | Risk | Visible coverage | Evidence |
|---|---|---|---|
| YAML contract loading | duplicate loaders diverge | COVERED | direct mapping parse through the single `_yaml_load` implementation |
| Closeout artifact writer | phase or verdict contract changes during rename | COVERED | direct temp-run assertion on path, phase, verdict and output |
| Nested gates before/after execution | incorrect ordering or status propagation | COVERED | existing executor tests characterize both gate positions |
| Depth and cycle protection | recursive execution escapes its boundary | COVERED | existing tests assert depth progression and cycle blocking |
| Missing contract and static gate | invalid input produces false success | COVERED | existing negative and expected-status cases |

## Visible coverage status

- `tests/test_executor.py`: 10 tests pass after adding direct coverage for the
  YAML loader and closeout writer.
- `mypy tools/vbb-executor.py --ignore-missing-imports`: zero errors, down from
  the 34-error bounded baseline.
- The production module contains one `_yaml_load` definition and no
  `write_closEOUT` symbol.

## Priority gaps

| Priority | Gap | Impact |
|---|---|---|
| P2 | CLI `cmd_run` artifact emission is covered through its components, not one end-to-end subprocess | a future CLI wiring regression could escape the focused suite |
| P3 | Duration and log-state formatting have no deterministic clock characterization | low-risk diagnostic drift |

No P1 critical path remains uncovered in this scope.

## Top tests recommended next

1. Execute `run` against a fixture contract and assert both generated artifacts
   plus the structured stdout payload.
2. Freeze time around one execution to characterize duration and state-log
   formatting if those diagnostics become a compatibility surface.

## Unknowns / evidence limits

- External scripts importing private executor helpers are not a supported API;
  repository search found no consumer of the renamed writer.
- Coverage percentage was intentionally not measured; the assessment follows
  behavioral risk and direct critical-path evidence.

# Documentation context harmonization — READY Wave 4

## Verdict

`READY` — current truth is globally identifiable. Two bounded documentation
gaps remain actionable without rewriting history: archive the loose historical
routing note and add one explicit responsibility matrix to the canonical prompt
architecture guide.

## Inventory (by class)

| Class | Surfaces | Role |
|---|---|---|
| CANONICAL | `docs/CONTEXT.md`, `docs/PILOTAGE.md`, `PROMPTS_ARCHITECTURE.md` | current entrypoints and prompt-layer narrative |
| OPERATIONAL | `docs/router/ROUTER_MATRIX.md`, `prompts/t-p-vbb-phase-router.md` | detailed selection and executable router prompt |
| VERSIONED | `docs/runs/*/` directories | timestamped work evidence |
| REPORT | `docs/audits/*.md` | immutable findings and verification evidence |
| EPHEMERAL/HISTORICAL | `docs/runs/routing-fix-verification.md` | 2026-05-28 pending manual protocol, not an active run |
| ARCHIVE | `docs/archive/` and thematic subdirectories | preserved superseded/historical material |

## Proposed canonical structure

- Keep `PROMPTS_ARCHITECTURE.md` as the single human-facing explanation of the
  four responsibilities: canonical phase prompts, specialized entrypoints,
  router, and installed short names.
- Keep `docs/router/ROUTER_MATRIX.md` as the detailed phase/context lookup; link
  it rather than duplicating its complete table.
- Keep `prompts/t-p-vbb-phase-router.md` as the executable decision prompt.
- Move the loose routing verification note unchanged to
  `docs/archive/runs/2026-05-28-routing-fix-verification.md`.

## Drift & contradictions

1. `docs/runs/routing-fix-verification.md` is `PENDING`, is not a directory and
   cannot satisfy the current run contract. The dashboard intentionally ignores
   it; QOA-006 tracks the ambiguity.
2. `PROMPTS_ARCHITECTURE.md` describes all prompt layers in separate sections
   but lacks one compact ownership/precedence matrix, leaving DOC-001 open.
3. Historical audits link the loose path as evidence. They are immutable and
   must not be rewritten; Git preserves the move and the active status will link
   the archive destination.
4. The inventory is coherent at 7 canonical files + 25 specialized root files +
   one root router file. No counter correction is required.

## Compression plan (max 10 steps)

1. Create `docs/archive/runs/` if absent.
2. Move the loose note byte-for-byte; add no historical interpretation inside.
3. Verify no loose Markdown remains under `docs/runs/` except its README.
4. Close QOA-006 with old/new paths and Git evidence.
5. Add a four-row responsibility/precedence matrix to
   `PROMPTS_ARCHITECTURE.md`.
6. Link the detailed router matrix from that compact table.
7. Verify every installed short name resolves to an existing prompt.
8. Close DOC-001 only after link and prompt inventory checks.
9. Run Markdown link checks and P.R2 per run.

## Archive policy proposal

- Archive destination: `docs/archive/<theme>/YYYY-MM-DD-<slug>.md`.
- Preserve content and Git ancestry; do not rewrite historical status or dates.
- Active truth must link the archive only as evidence, never as current
  operational instruction.
- Historical audits retain their original links and wording.
- No deletion: archive moves are non-destructive reclassification.

## Unknowns / needs confirmation

- The original Pi session checklist was never completed; UNKNOWN remains
  historical and does not become a current verification claim.
- External bookmarks to the old loose path cannot be inventoried. Repository
  operational references do not depend on it.

```yaml
FINAL_STATUS:
  elapsed_seconds: 90
  budget_initial: 180
  progress_emitted: false
  progress_count: 0
  extension_requested: false
  timeout_closeout_emitted: false
  verdict: COMPLETE
  files_touched:
    - docs/audits/doc-context-20260714-1545.md
    - docs/AUDIT_STATUS.md
  tests_run:
    - prompt inventory
    - repository reference search
  tests_missing:
    - archive move verification
    - responsibility matrix verification
  risks:
    - QOA-006
    - DOC-001
  open_points:
    - execute two separate documentation runs
```
